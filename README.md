# tkn.land

Mobile-first PWA wallet. See [PLAN.md](./PLAN.md).

## Develop

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
pnpm preview
```

Static output lands in `dist/` (host on Vercel; Njalla later if needed).
